<form method="post" action="{{ path('app_lostitem_delete', {'id': lostitem.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lostitem.id) }}">
    <button class="btn">Delete</button>
</form>
