{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                {% for flash_error in app.flashes('reset_password_error') %}
                    <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
                {% endfor %}
                <h1 class="text-center mb-4">Reset your password</h1>

                {{ form_start(requestForm) }}
                    {{ form_row(requestForm.email, {'attr': {'class': 'form-control mb-3', 'placeholder': 'Enter your email address'}}) }}
                    <div class="mb-3">
                        <small>
                            Enter your email address, and we will send you a
                            link to reset your password.
                        </small>
                    </div>

                    <button class="btn btn-primary btn-block">Send password reset email</button>
                {{ form_end(requestForm) }}
            </div>
        </div>
    </div>
{% endblock %}
