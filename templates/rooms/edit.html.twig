{% extends 'base.html.twig' %}

{% block title %}Edit Rooms{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Edit Rooms</h1>

    <div class="row">
        <div class="col-md-6">
            {{ include('rooms/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-md-6">
            <a href="{{ path('app_rooms_index') }}" class="btn btn-secondary">Back to list</a>
        </div>
        <div class="col-md-6">
            {{ include('rooms/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
