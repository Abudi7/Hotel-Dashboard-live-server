{% extends 'base.html.twig' %}

{% block title %}New Rooms{% endblock %}

{% block body %}
<div class="container">
    <h1 class="mt-4">Create new Room</h1>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    {{ include('rooms/_form.html.twig') }}
                </div>
            </div>
            <button type="button" class="btn btn-secondary" onclick="window.history.back();">
                        <i class="fas fa-arrow-left"></i> Back
            </button>
        </div>
    </div>
</div>
{% endblock %}
